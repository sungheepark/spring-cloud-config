token:
  expiration_time: 864000000 #하루짜리 토큰  millisecond
  secret: my_token #user_token_ecommerce         #임의값

gateway:
  ip: 127.0.0.1  

# $   git  remote   -v 
# $   git  remote   add   origin   https://github.com/sungheepark/spring-cloud-config.git
# $   git  push   --set-upstream   origin   master